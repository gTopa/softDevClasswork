Animation

requestAnimationFrame

Why? -> requestAnimationFrame tells browser to run a specified function before drawing next frame, browser will attempt to run ~60 fps

Lets browser handle when code should be executed, lets browser pause animation when page not in foreground

window.requestAnimationFrame( <FUNCTION> )